specVersion: "v1beta"
serviceId: "fallAlert-capstone"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "fallAlert-capstone-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
